language: node_js

node_js:
  - 8

branches:
  only:
    - master

jobs:
  include:
    - stage: npm release
      node_js: 8
      before_deploy:
        - npm run sass
      deploy:
        provider: npm
        email: "$NPM_EMAIL"
        api_key: "$NPM_TOKEN"
        github_token: "$GITHUB_TOKEN"
        on:
          branch: master
        skip_cleanup: true
      script:
        - npx semantic-release
